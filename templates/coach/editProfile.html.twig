{% extends 'base.html.twig' %}

{% block title %}Edit Profile{% endblock %}

{% block body %}
    <h1>Edit Profile</h1>

    <form method="post">
        <label for="nom">Name:</label>
        <input type="text" id="nom" name="nom" value="{{ coach.nom }}" required>

        <label for="telephone">Phone:</label>
        <input type="number" id="telephone" name="telephone" value="{{ coach.telephone }}" required>

        <label for="image">Image:</label>
        <input type="text" id="image" name="image" value="{{ coach.image }}" required>

        <label for="email">Email:</label>
        <input type="email" id="email" name="email" value="{{ coach.email }}" required>

        <button type="submit">Save</button>
    </form>
{% endblock %}